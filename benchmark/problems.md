
## URL Shortener with Analytics

Problem source: https://render.com/blog/ai-coding-agents-benchmark

> Please build a simple url shortener app. Please build it in nextjs with a minimalist style using the mui component library. The app should have a single input field that takes in a URL from the user and returns a shortened/encoded url. For the backend, provide a postgres connection for connecting to a database and storing the shortened urls. The app should be deployable via a dockerfile.

## Todo App with Undo/Redo

> Build a Todo app with support for undoing and redoing actions (add, delete, toggle).

## Password Generator

> Generate secure passwords with customizable options.

##  File Search Tool (grep-like)

> Build a CLI tool that searches for a pattern in files recursively.

## CPU/Memory Monitor

> Display real-time system resource usage in the terminal.

## Morse Code Translator

> Encode and decode text to/from Morse code.